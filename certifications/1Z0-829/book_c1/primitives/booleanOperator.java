public class booleanOperator {

  boolean f;// as a class var the defaul values is false;
  Boolean b;// the deafulst vauel is null

  // In Java, local variables (variables declared within a method) must be initialized before they are
  // used

  public static void main(String[] args) {
    boolean ff;
    Boolean g;
    booleanOperator h = new booleanOperator();
    System.out.println(h.f);
    // System.out.println(g.booleanValue());
  }
}
